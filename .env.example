# Database
DB_USER=racenotes
DB_PASSWORD=racenotes
DB_NAME=racenotes

# JWT
JWT_SECRET=change-me-to-a-random-secret

# App
APP_PORT=80

# SMTP (optional, for email reminders)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
